import React, { useState } from 'react'
import { useParams } from "react-router";
import customers from '../../apis/customers';


const Delete = (props)=>{

        return (<div>Delete Customer</div>)
}


export default Delete;